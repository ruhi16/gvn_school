@php
    $panelTitles = ['overview' => 'Student overview', 'profile' => 'My profile', 'school' => 'School information', 'academic' => 'My academics', 'exams' => 'Exam results', 'notices' => 'School notices'];
    $address = $student ? collect([$student->village, $student->district, $student->state])->filter()->join(', ') : null;
@endphp

<div class="min-h-[calc(100vh-8rem)] bg-[#fffdf3] text-stone-900">
    <div class="mx-auto flex max-w-[1600px] flex-col overflow-hidden rounded-xl border border-amber-200 bg-white shadow-sm lg:flex-row">
        <aside class="w-full shrink-0 bg-[#713f12] text-amber-50 lg:min-h-[calc(100vh-8rem)] lg:w-64">
            <div class="flex h-16 items-center gap-3 border-b border-amber-900 px-5"><div class="grid h-9 w-9 place-items-center rounded-lg bg-yellow-300 text-sm font-black text-amber-950">GV</div><div><p class="text-xs font-bold tracking-[0.18em] text-white">GVN SCHOOL</p><p class="text-[10px] uppercase tracking-widest text-amber-200">Student workspace</p></div></div>
            <nav class="grid gap-1 p-3 text-sm sm:grid-cols-3 lg:block">
                <p class="px-3 pb-1 pt-2 text-[10px] font-bold uppercase tracking-[0.18em] text-amber-200 sm:col-span-3 lg:pt-3">Workspace</p>
                @foreach ([['overview', 'Overview'], ['profile', 'My profile'], ['school', 'School information']] as [$panel, $label])
                    <button wire:click="selectPanel('{{ $panel }}')" class="w-full rounded-lg px-3 py-2.5 text-left transition {{ $activePanel === $panel ? 'bg-yellow-300 font-semibold text-amber-950 shadow-sm' : 'text-amber-50 hover:bg-amber-900' }}">{{ $label }}</button>
                @endforeach
                <p class="px-3 pb-1 pt-4 text-[10px] font-bold uppercase tracking-[0.18em] text-lime-200 sm:col-span-3 lg:pt-7">Learning</p>
                @foreach ([['academic', 'My academics'], ['exams', 'Exam results'], ['notices', 'Notices']] as [$panel, $label])
                    <button wire:click="selectPanel('{{ $panel }}')" class="w-full rounded-lg px-3 py-2.5 text-left transition {{ $activePanel === $panel ? 'bg-lime-300 font-semibold text-lime-950 shadow-sm' : 'text-amber-50 hover:bg-amber-900' }}">{{ $label }}</button>
                @endforeach
            </nav>
            <div class="mx-4 mt-3 rounded-lg border border-amber-800 bg-amber-950/50 p-3 text-xs sm:hidden lg:block"><p class="font-semibold text-yellow-300">View and update</p><p class="mt-1 leading-relaxed text-amber-200">Contact details and your profile image can be updated here. Academic records remain protected.</p></div>
        </aside>

        <section class="min-w-0 flex-1 bg-[#fffdf3]">
            <header class="flex flex-wrap items-center justify-between gap-3 border-b border-amber-100 bg-white px-5 py-4 sm:px-7"><div><p class="text-[10px] font-bold uppercase tracking-[0.18em] text-amber-700">Student workspace</p><h1 class="text-xl font-semibold tracking-tight text-stone-950">{{ $panelTitles[$activePanel] ?? 'Student overview' }}</h1></div><div class="flex items-center gap-2"><span class="rounded-full bg-lime-50 px-3 py-1.5 text-xs font-semibold text-lime-700">VIEW ONLY ACADEMICS</span><span class="rounded-full bg-yellow-50 px-3 py-1.5 text-xs font-semibold text-amber-800">{{ $session?->name ?? 'No active session' }}</span></div></header>
            <main class="space-y-5 p-5 sm:p-7">
                @if (session('success'))<div class="rounded-lg border border-emerald-200 bg-emerald-50 px-4 py-3 text-sm text-emerald-700">{{ session('success') }}</div>@endif
                @if (!$student)
                    <div class="rounded-lg border border-amber-200 bg-amber-50 p-4 text-sm text-amber-900">Verify your student profile with your date of birth to view and update your information.</div>
                    <div class="rounded-lg border border-amber-200 bg-white p-5"><livewire:current-profile-comp /></div>
                @else
                    @if ($activePanel === 'overview' || $activePanel === 'academic')
                        <div class="grid grid-cols-2 gap-3 xl:grid-cols-4">@foreach ([['Class records', $classRecords->count(), 'text-amber-800', 'bg-yellow-100'], ['Subjects', $subjectCount, 'text-lime-800', 'bg-lime-100'], ['Exams', $examCount, 'text-orange-800', 'bg-orange-100'], ['Mark entries', $marksTotal, 'text-rose-800', 'bg-rose-100']] as [$label, $value, $color, $background])<div class="rounded-lg border border-amber-100 bg-white p-4"><div class="mb-3 grid h-8 w-8 place-items-center rounded {{ $background }} text-xs font-black {{ $color }}">{{ substr($label, 0, 1) }}</div><p class="text-xs text-stone-500">{{ $label }}</p><p class="mt-1 text-2xl font-semibold {{ $color }}">{{ number_format($value) }}</p></div>@endforeach</div>
                    @endif
                    @if ($activePanel === 'overview' || $activePanel === 'profile')
                        <div class="grid gap-5 xl:grid-cols-[1.15fr_0.85fr]"><section class="rounded-lg border border-amber-200 bg-white p-5"><div class="flex items-start justify-between gap-4"><div class="flex items-center gap-4">@if ($student->dp_img_ref)<img src="{{ asset('storage/' . $student->dp_img_ref) }}" alt="{{ $student->name }}" class="h-16 w-16 rounded-full object-cover ring-4 ring-yellow-100">@else<div class="grid h-16 w-16 place-items-center rounded-full bg-yellow-100 text-xl font-bold text-amber-800 ring-4 ring-yellow-50">{{ strtoupper(substr($student->name, 0, 1)) }}</div>@endif<div><p class="text-[10px] font-bold uppercase tracking-[0.18em] text-amber-700">Your profile</p><h2 class="mt-1 text-lg font-semibold text-stone-950">{{ $student->name }}</h2></div></div><div class="flex items-center gap-2"><span class="rounded-full bg-yellow-50 px-2.5 py-1 text-[10px] font-bold text-amber-800">STUDENT</span><button wire:click="openProfileEditor" class="rounded-lg bg-yellow-300 px-3 py-2 text-xs font-bold text-amber-950 hover:bg-yellow-200">Edit details</button></div></div><dl class="mt-5 grid gap-4 text-sm sm:grid-cols-2"><div><dt class="text-xs text-stone-500">Father / guardian</dt><dd class="mt-1 font-medium">{{ $student->fname ?: 'Not available' }}</dd></div><div><dt class="text-xs text-stone-500">Mother / guardian</dt><dd class="mt-1 font-medium">{{ $student->mname ?: 'Not available' }}</dd></div><div><dt class="text-xs text-stone-500">Date of birth</dt><dd class="mt-1 font-medium">{{ $student->dob?->format('d M Y') ?: 'Not available' }}</dd></div><div><dt class="text-xs text-stone-500">Gender</dt><dd class="mt-1 font-medium">{{ $student->gender ?: 'Not available' }}</dd></div><div><dt class="text-xs text-stone-500">Primary mobile</dt><dd class="mt-1 font-medium">{{ $student->mobile_1 ?: 'Not available' }}</dd></div><div><dt class="text-xs text-stone-500">Secondary mobile</dt><dd class="mt-1 font-medium">{{ $student->mobile_2 ?: 'Not available' }}</dd></div><div><dt class="text-xs text-stone-500">Email</dt><dd class="mt-1 font-medium">{{ $student->email ?: 'Not available' }}</dd></div><div><dt class="text-xs text-stone-500">Nationality</dt><dd class="mt-1 font-medium">{{ $student->nationality ?: 'Not available' }}</dd></div><div class="sm:col-span-2"><dt class="text-xs text-stone-500">Address</dt><dd class="mt-1 font-medium">{{ $address ?: 'Not available' }}</dd></div><div><dt class="text-xs text-stone-500">Post office</dt><dd class="mt-1 font-medium">{{ $student->post_office ?: 'Not available' }}</dd></div><div><dt class="text-xs text-stone-500">Police station</dt><dd class="mt-1 font-medium">{{ $student->police_station ?: 'Not available' }}</dd></div></dl><p class="mt-5 text-xs text-stone-400">Name, date of birth, gender, parent details, admission, school, session, and academic records can be changed by an administrator only.</p></section><section class="rounded-lg border border-amber-200 bg-white p-5"><p class="text-[10px] font-bold uppercase tracking-[0.18em] text-lime-700">School context</p><h2 class="mt-1 text-lg font-semibold">{{ $school?->name ?? 'School not assigned' }}</h2><dl class="mt-5 space-y-3 text-sm"><div class="flex justify-between gap-4 border-b border-amber-100 pb-3"><dt class="text-stone-500">Active session</dt><dd class="text-right font-medium">{{ $session?->name ?? 'Not configured' }}</dd></div><div class="flex justify-between gap-4 border-b border-amber-100 pb-3"><dt class="text-stone-500">Student status</dt><dd class="text-right font-medium">{{ $student->is_active ? 'Active' : 'Inactive' }}</dd></div><div class="flex justify-between gap-4"><dt class="text-stone-500">Class records</dt><dd class="text-right font-medium">{{ $classRecords->count() }}</dd></div></dl></section></div>
                    @endif
                    @if ($activePanel === 'school')<section class="rounded-lg border border-amber-200 bg-white p-5"><p class="text-[10px] font-bold uppercase tracking-[0.18em] text-amber-700">School information</p><h2 class="mt-1 text-xl font-semibold">{{ $school?->name ?? 'School not assigned' }}</h2><div class="mt-6 grid gap-4 text-sm sm:grid-cols-2 lg:grid-cols-3">@foreach ([['DISE code', $school?->dise_code], ['UDISE code', $school?->udise_code], ['Type', $school?->school_type], ['Village / town', $school?->vill], ['District', $school?->district], ['Session', $session?->name]] as [$label, $value])<div class="rounded-lg bg-yellow-50 p-4"><p class="text-xs text-stone-500">{{ $label }}</p><p class="mt-1 font-semibold text-stone-800">{{ $value ?: 'Not available' }}</p></div>@endforeach</div></section>@endif
                    @if ($activePanel === 'overview' || $activePanel === 'academic')<section class="rounded-lg border border-amber-200 bg-white p-5"><p class="text-[10px] font-bold uppercase tracking-[0.18em] text-lime-700">Current class records</p><h2 class="mt-1 text-lg font-semibold">Academic placement</h2><div class="mt-4 divide-y divide-amber-100">@forelse ($classRecords as $record)<div class="flex flex-wrap items-center justify-between gap-3 py-3 text-sm"><div><p class="font-medium">{{ $record->shreny?->name ?? 'Class not available' }} <span class="text-stone-400">/</span> {{ $record->section?->name ?? 'Section not available' }}</p><p class="mt-1 text-xs text-stone-500">Roll number: {{ $record->curr_roll_no ?: 'Not assigned' }}</p></div><span class="rounded-full bg-lime-50 px-2.5 py-1 text-xs text-lime-700">{{ $record->is_promoted ? 'Promoted' : 'Current' }}</span></div>@empty<p class="py-4 text-sm text-stone-500">No academic placement has been assigned yet.</p>@endforelse</div></section>@endif
                    @if ($activePanel === 'overview' || $activePanel === 'exams')<section class="rounded-lg border border-amber-200 bg-white p-5"><p class="text-[10px] font-bold uppercase tracking-[0.18em] text-orange-700">Exam results</p><h2 class="mt-1 text-lg font-semibold">Your recorded marks</h2><div class="mt-5 rounded-lg bg-orange-50 p-4"><p class="text-xs text-orange-700">Mark entries linked to your current class record</p><p class="mt-1 text-3xl font-semibold text-orange-900">{{ number_format($marksTotal) }}</p></div><p class="mt-4 text-xs text-stone-500">Detailed marks remain view-only and are maintained by authorized school staff.</p></section>@endif
                    @if ($activePanel === 'overview' || $activePanel === 'notices')<section class="rounded-lg border border-amber-200 bg-white p-5"><p class="text-[10px] font-bold uppercase tracking-[0.18em] text-rose-700">School notices</p><h2 class="mt-1 text-lg font-semibold">Latest updates</h2><div class="mt-4 divide-y divide-amber-100">@forelse ($notices as $notice)<article class="py-3"><div class="flex flex-wrap items-center justify-between gap-2"><h3 class="font-medium">{{ $notice->title }}</h3><time class="text-xs text-stone-500">{{ $notice->upload_dt?->format('d M Y') }}</time></div><p class="mt-1 text-sm leading-relaxed text-stone-600">{{ $notice->description ?: 'No additional details.' }}</p></article>@empty<p class="py-4 text-sm text-stone-500">No active notices for this school and session.</p>@endforelse</div></section>@endif
                @endif
            </main>
        </section>
    </div>
</div>

@if ($showProfileEditor && $student)
    <div class="fixed inset-0 z-50 grid place-items-center bg-stone-950/50 px-4" role="dialog" aria-modal="true"><div class="max-h-[90vh] w-full max-w-3xl overflow-y-auto rounded-xl bg-white p-5 shadow-xl sm:p-6"><div class="flex items-start justify-between gap-4 border-b border-amber-100 pb-4"><div><p class="text-[10px] font-bold uppercase tracking-[0.18em] text-amber-700">Self-service profile</p><h2 class="mt-1 text-lg font-semibold">Update your details</h2></div><button wire:click="$set('showProfileEditor', false)" class="text-2xl leading-none text-stone-400" aria-label="Close">&times;</button></div>@error('profile')<p class="mt-4 rounded-lg bg-rose-50 px-3 py-2 text-xs text-rose-700">{{ $message }}</p>@enderror<div class="mt-5 grid gap-4 sm:grid-cols-2"><div class="sm:col-span-2"><label class="text-xs font-medium text-stone-600">Profile image</label><input type="file" wire:model="profileImage" accept="image/*" class="mt-1 block w-full rounded-lg border border-amber-200 px-3 py-2 text-sm">@error('profileImage')<p class="mt-1 text-xs text-rose-600">{{ $message }}</p>@enderror</div>@foreach ([['mobile_1', 'Primary mobile'], ['mobile_2', 'Secondary mobile'], ['email', 'Email'], ['village', 'Village / town'], ['post_office', 'Post office'], ['police_station', 'Police station'], ['district', 'District'], ['block', 'Block'], ['pincode', 'Pincode'], ['state', 'State'], ['nationality', 'Nationality']] as [$field, $label])<div><label class="text-xs font-medium text-stone-600">{{ $label }}</label><input wire:model="{{ $field }}" type="{{ $field === 'email' ? 'email' : 'text' }}" class="mt-1 w-full rounded-lg border-amber-200 text-sm">@error($field)<p class="mt-1 text-xs text-rose-600">{{ $message }}</p>@enderror</div>@endforeach</div><div class="mt-6 flex justify-end gap-2 border-t border-amber-100 pt-4"><button wire:click="$set('showProfileEditor', false)" class="rounded-lg border border-amber-200 px-4 py-2 text-xs font-semibold text-stone-700">Cancel</button><button wire:click="saveProfile" wire:loading.attr="disabled" class="rounded-lg bg-yellow-300 px-4 py-2 text-xs font-bold text-amber-950 hover:bg-yellow-200">Save profile</button></div></div></div>
@endif